# 전역 설정값 정의

# 지원 심볼 목록 (선물 거래 기준)
SYMBOLS = ["BTCUSDT", "ETHUSDT", "XRPUSDT", "SOLUSDT"]

# 지원 인터벌 목록 (1M = 1달, 대문자 주의)
INTERVALS = ["1m", "5m", "15m", "1h", "4h", "1d", "1w", "1M"]

# 기본 인터벌
DEFAULT_INTERVAL = "5m"

# 바이낸스 선물 WebSocket 베이스 URL
BINANCE_WS_BASE = "wss://fstream.binance.com"
